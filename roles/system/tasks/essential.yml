---
- name: Show username
  ansible.builtin.debug:
    var: ansible_user_id

- name: Update pacman package cache
  community.general.pacman:
    update_cache: true

- name: Upgrade pacman packages when explicitly requested
  community.general.pacman:
    upgrade: true
  when: upgrade_packages | default(false) | bool
